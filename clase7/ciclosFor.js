// Ciclos
// FOR

for (let index = 0; index < 10; index++) {
    console.log("cantidad index", index);
}
// for (let index = 10 ; index >= 1 ; index --){console.log ("cantidad index", index);}

// console.log("elemento array", series [1]);
let series = ["game of thrones","los simuladores", "los simpssons","el encargado","breaking bad"]

for (let index = 0; index < series.length; index++)
    {console.log("la serie", series[index])}

console.log("vamos a itera", series.length, "veces");
for (let index = 0; index < series.length; index++) {
    console.log("esta es la vuelta"+ index, "la serie", series[index])}
